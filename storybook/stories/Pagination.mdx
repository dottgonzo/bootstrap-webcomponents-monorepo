<!-- Pagination.mdx -->

import { Story, ArgsTable } from "@storybook/addon-docs";
import pkg from "../../lerna.json";
import meta from "./Pagination.stories";
import { fromMetaArgsTypesToHtmlTable } from "./utils";

# Pagination

Pagination is a basic component.

## dependency

<pre><code>
  {`<script
    id="paginationbootstrapcomponentjs"
    src="https://cdn.jsdelivr.net/npm/@htmlbricks/paginationbootstrap-component@${pkg.version}/release/paginationbootstrap.js"
  ></script>`}

</code></pre>

## attributes

{meta ? fromMetaArgsTypesToHtmlTable(meta.argTypes) : ""}

<>{JSON.stringify(meta.argTypes)}</>
<streamingtablebootstrap-webcomponent
  rows=""
  header='<>{meta ? [] : ""}</>'
></streamingtablebootstrap-webcomponent>

## css variables

## slots

## examples

- Basic

```
<paginationbootstrap-component />
```

<paginationbootstrap-component />

- With 5 pages

```
<paginationbootstrap-component pages="5" />
```

<paginationbootstrap-component pages="5" />

- With 12 pages on page 3

```
<paginationbootstrap-component pages="12" page="3" />
```

<paginationbootstrap-component pages="12" page="3" />
