<!-- Pagination.mdx -->

import { Story, ArgsTable } from "@storybook/addon-docs";
import pkg from "../../lerna.json";
import meta from "./Pagination.stories";
import { fromMetaArgsTypesToHtmlTable } from "./utils";

# Pagination

Pagination is a basic component.

## dependency

<pre><code>
  {`<script
    id="paginationbootstrapcomponentjs"
    src="https://cdn.jsdelivr.net/npm/@htmlbricks/hb-paginate@${pkg.version}/release/paginationbootstrap.js"
  ></script>`}

</code></pre>

## attributes

{meta ? fromMetaArgsTypesToHtmlTable(meta.argTypes) : ""}

<>{JSON.stringify(meta.argTypes)}</>
<hb-table rows="" header='<>{meta ? [] : ""}</>'></hb-table>

## css variables

## slots

## examples

- Basic

```
<hb-paginate />
```

<hb-paginate />

- With 5 pages

```
<hb-paginate pages="5" />
```

<hb-paginate pages="5" />

- With 12 pages on page 3

```
<hb-paginate pages="12" page="3" />
```

<hb-paginate pages="12" page="3" />
