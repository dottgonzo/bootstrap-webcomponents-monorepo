{
  "name": "root",
  "private": "true",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "install-packages": "npx lerna bootstrap --hoist",
    "build:release": "npx lerna run build:release",
    "publish:patch": "npx lerna publish patch --force-publish --yes",
    "publish:minor": "npx lerna publish patch --force-publish --yes",
    "publish:major": "npx lerna publish major --force-publish --yes"
  }
}
