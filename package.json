{
  "name": "root",
  "private": "true",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "install-packages": "npx lerna bootstrap --hoist",
    "build:release": "npx lerna run build:release",
    "publish:patch": "npm run build:release && npx lerna publish patch --ignore-prepublish --force-publish --yes",
    "publish:minor": "npm run build:release && npx lerna publish patch --ignore-prepublish --force-publish --yes",
    "publish:major": "npm run build:release && npx lerna publish major --ignore-prepublish --force-publish --yes"
  }
}
